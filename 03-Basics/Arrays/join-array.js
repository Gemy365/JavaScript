console.log('Join between array of char') //Done!

const arrOfName = ['G', 'e', 'm', 'y']

const joinArray = function (arrOfName) {
    return arrOfName.join('') // join --> Adding chars together
}

const joinName = joinArray(arrOfName) 
console.log(joinName)
